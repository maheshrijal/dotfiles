# Generated by `chezmoi init` from home/.chezmoi.toml.tmpl — do not edit by hand.
sourceDir = {{ (joinPath .chezmoi.homeDir "code" "dotfiles") | quote }}

# Git commit signing is intentionally not configured during bootstrap.
# After GPG is set up on a machine, add this manually and run `chezmoi apply`:
#
# [data]
#     gitSigningKey = "YOUR_KEY_FINGERPRINT"
